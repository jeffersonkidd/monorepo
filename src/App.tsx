import { AllProviders } from "data";
import { MarketingTemplate } from "MarketingTemplate";

function App() {
  return (
    <AllProviders>
      <MarketingTemplate />
    </AllProviders>
  );
}

export default App;